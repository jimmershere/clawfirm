# Core skills

The minimum-viable, always-signed skill set ClawFirm ships with. These are intentionally narrow — read-only filesystem, allowlisted web search, read-only git inspection. Anything more powerful is opt-in from the broader registry.

Source for each skill lives in its own subdirectory and is built/signed by the release pipeline; this README is the index.
