# Threat model

Detailed threat model for ClawFirm. The summary lives in [`../../SECURITY.md`](../../SECURITY.md); this document expands each threat with attacker capabilities, mitigations, and detection.

Coming in Q3-26. The high-level threat model in `SECURITY.md` is the canonical reference until then.
