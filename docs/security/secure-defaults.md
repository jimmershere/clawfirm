# Secure-default matrix

The full secure-default matrix lives in [`../../SECURITY.md`](../../SECURITY.md#secure-default-matrix). This document records changes to the matrix over time, with rationale and the ADR that drove each change.
